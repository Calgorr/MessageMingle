apiVersion: v1
kind: Service
metadata:
  name: {{ include "messageMingle.fullname" . }}
  labels:
    {{- include "messageMingle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "messageMingle.selectorLabels" . | nindent 4 }}
