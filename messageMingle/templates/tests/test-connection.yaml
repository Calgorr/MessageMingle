apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "messageMingle.fullname" . }}-test-connection"
  labels:
    {{- include "messageMingle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "messageMingle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
